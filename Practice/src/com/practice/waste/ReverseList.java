package com.practice.waste;
/*package com.practice.interview;

import com.practice.interview.DoubleLinkedList.Node;

public class ReverseList {
	Node head;
	class Node{
		int val;
		Node prev,next;
		Node(int item){
			val=item;
			prev=next=null;
		
	}
		
	}
	void push(int listVal) {
		Node new_Node=new Node(listVal);
		new_Node.prev=null;
		new_Node.next=head;
		if(head!=null) {
			head=new_Node.prev;
		}
		
	}
public static void main(String[] args) {
	ReverseList list=new ReverseList();

     allocate node 
    Node new_node = new Node(new_data);

     since we are adding at the begining, 
     prev is always NULL 
    new_node.prev = null;

     link the old list off the new node 
    new_node.next = head;

     change prev of head node to new node 
    if (head != null) {
        head.prev = new_node;
    }

     move the head to point to the new node 
    head = new_node;

}
	}
*/